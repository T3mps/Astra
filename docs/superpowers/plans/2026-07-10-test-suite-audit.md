# Astra Test Suite Audit — 2026-07-10

Verdicts: OK | SPLIT (needs decomposition) | FIX (test-quality defect) | GAP (missing coverage)
Dispositions: A = covered by remediation Task N | B = fixed in this task | C = new item (appended to plan Deferred section or filed as follow-up)

| File | Tests | Lines | Verdict | Findings | Disposition |
|---|---|---|---|---|---|
| tests/TestMain.cpp | - | 7 | OK | Trivial `InitGoogleTest`/`RUN_ALL_TESTS` boilerplate. Nothing to find. | - |
| tests/TestComponents.hpp | - | 332 | FIX | Shared by ~19 test files. Grep confirms 4 of 16 component types are entirely unused by any TEST body: `Inventory`, `Timer`, `Hierarchy`, `Metadata` (0 references outside this file); `Static` and `Damage` are used in only 1 file each. Not deleted here (shared header, > 20-line blast radius across many consumers, and the brief asks to "flag" not fix) — see Deferred. Separately: `Health{int current = 100; int max = 100;}` is exactly the NSDMI-default shape Task 3 fixes (`is_trivially_copyable && is_nothrow_default_constructible` incorrectly skips construction and memsets to 0 in every config today). Confirmed no existing test default-constructs `Health` via `CreateEntity<Health>()`/`CreateEntity<..., Health>()` (found in `RelationsTest.cpp:374-375`) and then asserts `current`/`max` — those call sites only check component *presence*, so the live zeroing bug is currently invisible to the suite. | C (unused types) / A (Task 3 — DefaultConstructTest.cpp covers the Health-shaped NSDMI case) |
| tests/Commands/CommandBufferTest.cpp (was tests/TestCommandBuffer.cpp) | 12 | 350→351 | FIX, GAP | Relocated (was the only suite living at `tests/` root while every other suite is foldered). Fixed the resulting broken relative include (`"TestComponents.hpp"` → `"../TestComponents.hpp"`). All 12 tests have real, specific assertions (e.g. `RemoveComponents` checks the removed component is gone AND the adjacent component/entity are untouched). GAP: no test drives an invalid/dead entity through `CommandBuffer::SetParent/AddLink/AddComponent` (only `Registry`-level paths get this per Task 6); `CommandBuffer::MergeFrom` and `ParallelCommandBuffer::MergeInto/Clear/GetThreadCount` are never called (Public-API spot-check miss). | B (relocation + include fix, done) / C (MergeFrom/MergeInto/Clear/GetThreadCount + CommandBuffer-level invalid-entity rejection) |
| tests/Comprehensive/ComplexRelationshipTest.cpp | 9 | 561→577 | FIX | 8 of 9 tests are solid. `RapidRelationshipChanges` (one of Task 1's 6 former Debug-aborters) ran 1000 random `SetParent`/`RemoveParent`/`AddLink`/`RemoveLink` ops (fixed seed `std::mt19937 rng(12345)`) but only asserted "no crash" / "entities still valid" — never confirmed the cycle-rejection invariant actually held. Strengthened in place: added a post-loop ancestor-chain-termination check for every entity (walks `GetParent` up to `entities.size()` steps; `ASSERT_LE` on the step count) — if a cycle had ever slipped through silently, this now fails instead of passing on luck. No test-logic elsewhere changed. | B (strengthened, done) |
| tests/Comprehensive/ComponentLifecycleTest.cpp | 8 | 487 | FIX, GAP | `SideEffectsOrder` only checks event *presence*, never actual ordering despite the name (rubric 10 mismatch). Every test is happy-path — no negative/rejection test anywhere in the file (rubric 7 GAP). `ArchetypeCleanupLifecycle` discards the `DefragmentationResult` from `Registry::Defragment` (partial API hit only). | C |
| tests/Comprehensive/ErrorRecoveryTest.cpp | 9 | 389 | FIX | `PartialBatchCreationFailure`/`ComponentAdditionFailureRecovery` are named for induced failure but never actually induce one (comment concedes the failure "might" happen silently) — misleading test intent. `TrackedComponent::copyConstructorCalls`/`moveConstructorCalls` are incremented but never read by any EXPECT/ASSERT — write-only dead state. Genuine negative-path tests are present elsewhere in the file (`InvalidEntityOperations`, `ComponentRemovalDuringTransition`, `BatchDestructionWithInvalidEntities`). | C |
| tests/Comprehensive/MemoryCleanupTest.cpp | 7 | 353 | OK | Real/specific assertions throughout; `CleanupOptionsRespected` exercises constraint-rejection (negative path present). `Registry::Defragment`+`DefragmentationResult.archetypesRemoved` hit; `ArchetypeChunkPool::Defragment` not hit here. No dead helpers. | - |
| tests/Comprehensive/ResourceExhaustionTest.cpp | 10 | 476 | FIX | `ArchetypeProliferation` and `SimpleEntityLifecycle` have **zero** `EXPECT_`/`ASSERT_` calls (rubric 3 violation) — comments admit the API they wanted to check was removed / it's leftover crash-isolation scratch code. `MaxComponentTypes` and `EntityIDSpaceExhaustion` don't actually approach the limits their names claim. Genuine negative path present (`ArchetypeChunkPoolExhaustion`). | C |
| tests/Container/AlignedStorageTest.cpp | 8 | 195 | OK | Real/specific assertions incl. byte-reinterpretation and void specialization. Compile-time layout utility — rubric 7 (negative path) is N/A, not a gap. No dead helpers. | - |
| tests/Container/BitmapFuzzTest.cpp | 4 | 78 | OK | Fixed literal seeds (`0xB17B17`, `0xCAFE`, `0xFAC4DE`). Oracle: `std::bitset<128>` compared every op. `OutOfBoundsIsNoop` is the negative/boundary test. Meets both fuzz-test special checks. | - |
| tests/Container/BitmapTest.cpp | 10 | 317 | OK | Real/specific assertions; `BoundaryConditions` is the negative/boundary test (out-of-range `Set` silently rejected). No RNG, no dead helpers. | - |
| tests/Container/FlatMapFuzzTest.cpp | 7 | 154 | OK | Fixed literal seeds (`0xA57A1/2/3`, `s*7919`, `0xBADBAD`, `0xC011DE`). Oracle: `std::unordered_map` compared every op plus a deep parity sweep every 257 ops. `ReinsertAfterTombstoneDoesNotDuplicate` is a precise regression test with strong adjacent-state specificity. Meets both fuzz-test special checks. | - |
| tests/Container/FlatMapTest.cpp | 29→23 | 851→~510 | SPLIT | 29 tests / 851 lines spans two themes: public-API CRUD (insert/find/erase/emplace/iteration/copy/move/capacity/const-correctness) vs. SwissTable-internals (tombstones, rehashing, group-boundary handling, collision handling, randomized differential stress). Trivial 5-line fixture (`SetUp`/`TearDown` both empty) — safe to duplicate. Split: extracted `TombstoneHandling`, `RehashingBehavior`, `CollisionHandling`, `GroupBasedOperations`, `IteratorAcrossGroups`, `RandomOperations` (6 tests, byte-identical bodies) into new `tests/Container/FlatMapInternalsTest.cpp`; trimmed now-unused includes (`<algorithm>`, `<chrono>`, `<random>`, `<set>`) from the original. Suite name unchanged (`FlatMapTest` in both files — no gtest-visible rename). | B (done) |
| tests/Container/FlatSetFuzzTest.cpp | 8 | 165 | OK | Fixed literal seeds (`0x5E7A1/2/3`, `s*6271`, `0xBADC0DE`, `0xC011DEF`). Oracle: `std::unordered_set<int>` checked every op plus a periodic full parity sweep. `EmplaceDuplicateRejected`/`ReinsertAfterTombstoneDoesNotDuplicate` are strong negative/regression tests with adjacent-state checks. Meets both fuzz-test special checks. | - |
| tests/Container/FlatSetTest.cpp | 15 | 459→455 | FIX | 4 unused includes (`<algorithm>`, `<chrono>`, `<random>`, `<set>` — only `<unordered_set>` is actually used). Good negative-path and adjacent-state coverage otherwise (duplicate-insert rejection, erase-of-nonexistent-key, "modifying copy shouldn't affect original"). | B (unused includes removed, done) |
| tests/Container/SmallVectorFuzzTest.cpp | 2 | 75 | OK | Fixed seeds (`0x5111A11`, `s*104729`). Oracle: `std::vector<int>` compared element-by-element after every op. Explicitly exercises the inline/spill boundary plus copy- and move-round-trip. Meets both fuzz-test special checks. | - |
| tests/Container/SmallVectorTest.cpp | 23 | 688 | OK | Borderline size (just under the 700-line/25-test threshold) but cohesive — not split. Inline↔spill (rubric 8) genuinely covered across `SmallBufferOptimization`, `ShrinkToFit`, `MoveOperations`, `Swap`. No misuse/boundary-rejection test anywhere in the file (no `EXPECT_DEATH`/assert-triggering case), though `at()`'s bounds check is `ASTRA_ASSERT`-based so classic throw-testing doesn't apply. | C (add boundary/misuse coverage; not urgent, no one-line mechanical fix available) |
| tests/Core/TypeContextTest.cpp | 6 | 78 | GAP | All 6 tests are happy-path — no test of ID-space exhaustion, the Debug-only hash-collision assert, or double-registration. Isolation risk: `PendingRegistrationsDrainIntoInstalledContext` installs a stack-local `TypeContext` as the process-global active context via `SetTypeContext(&fresh)` and restores it manually (not RAII/exception-safe) — currently safe only because the intervening check is `EXPECT_NE` rather than `ASSERT_`; a future edit adding an early-returning `ASSERT_` there would leave a dangling pointer installed process-wide for the rest of the binary's run. | C (RAII guard for context restore + exhaustion/collision/double-register negative tests) |
| tests/Core/TypeIDTests.cpp | 9 | 186→184 | FIX | `CompileTimeConstexpr` ended with `EXPECT_TRUE(true); // Test passes if it compiles` — a pure tautology; the real checks are the preceding `static_assert`s, which already gate compilation. Deleted the tautological line (test still "fails" via compile error if the static_asserts don't hold — a stronger guarantee, not a weaker one). Separately found but NOT fixed here (needs an actual computed reference value, not a mechanical deletion): `XXHashProducesExpectedValues` hardcodes correct reference values for the empty string and `"a"` but falls back to `EXPECT_NE(helloHash, 0u)` for `"Hello"` — a much weaker check for a test whose whole point is matching the XXHash spec. **Notable cross-file finding**: `TypeIDTests.cpp:8` and `tests/Registry/ViewIteratorTest.cpp:10-13` each declare an identically-shaped anonymous-namespace `struct Position { float x, y, z; };` linked into the same `AstraTest.exe`. `TypeID<T>::Hash()` hashes the compiler's pretty-printed type name, and MSVC/Clang/GCC render anonymous-namespace scope without a per-TU discriminator — these two distinct C++ types may hash identically and silently alias to the same `ComponentID` under the shared process-wide `DefaultTypeContext()`. Not empirically confirmed (would need a throwaway build dumping both hashes), but the mechanism is real and is exactly the "TypeID registration is process-global" risk this audit was asked to check for. | B (tautology deleted, done) / C (weak XXHash assertion; anonymous-namespace `Position` collision risk — recommend empirical verification as a follow-up, potentially a real product bug rather than only a test gap) |
| tests/Core/WorkSchedulerTest.cpp | 7 | 101 | FIX | All `TEST()` suites are named `WorkerPool` rather than `WorkScheduler` (mismatches the file name / header under test, rubric 1). All 7 tests have real, specific, exact-count assertions (e.g. `ProcessesEveryIndexExactlyOnce` checks all 100,000 indices individually). No sleep-based sync (condition variables only). `IWorkScheduler` has no failure mode to reject, so rubric 7 doesn't strictly apply; `ZeroCountIsNoop` is the closest edge-case analogue and is present. Rename deferred rather than done here — cosmetic, no coverage impact, and the brief's split/strengthen budget was prioritized elsewhere. | C (rename `TEST(WorkerPool, ...)` → `TEST(WorkScheduler, ...)`, 7 macro sites, zero behavior change) |
| tests/Entity/EntityManagerSerializationTest.cpp | 11 | 515 | GAP | All 11 tests use only the memory-target `BinaryWriter(buffer)`/`BinaryReader(buffer)` constructors — no test exercises the file-target path despite `EntityManager::Serialize`/`Deserialize` supporting both (rubric 8). No test drives the documented `Result::Err` path — every test only asserts `IsOk()` (rubric 7). Otherwise sound: fixed seed (`std::mt19937 rng(42)`), stateless fixture, correctly located. | C (add file-target round-trip + corrupted/truncated-buffer negative test) |
| tests/Entity/EntityManagerTest.cpp | 26 | 598→596 | FIX | `ValidateMethod` calls `pool.Validate()` 4× with **zero** assertions (rubric 3) — and `EntityManager::Validate()` is itself `#ifdef ASTRA_BUILD_DEBUG`-gated (a Release no-op), so combined with the missing assertions this test verifies nothing at all in Release while appearing to pass (rubric 4). Removed unused `#include <thread>`/`<chrono>` (dead weight, likely left over from a commented-out block) as a mechanical cleanup. No entity-ID-exhaustion or 16/64-bit-width coverage (expected — those are separate remediation tasks). | B (unused includes removed, done) / A (Task 6 — entity exhaustion; Task 12 — 16/64-bit widths) / C (add real assertions to `ValidateMethod`, or `#ifdef NDEBUG`-document why it's Debug-only) |
| tests/Entity/EntityTest.cpp | 21 | 449 | GAP | `EntityTraitsConcept` only `static_assert`s trait constants for `Traits32`/`Traits64` — never instantiates/exercises a running 16-bit or 64-bit `Entity`; all 21 tests run under the default 32-bit config only. Otherwise fully sound: real assertions incl. static_asserts, negative paths present (`VersionOverflow`, `IDMasking`), deterministic, stateless, correctly located. | A (Task 12 — non-32-bit entity configurations) |
| tests/Component/ComponentRegistryTest.cpp | 24 | 674 | OK | 674 lines/24 tests is at the size edge but cohesive (one ComponentDescriptor lifecycle theme) — not split. Real assertions throughout; negative paths present (`NonRegisteredComponent`, unknown-hash `IsErr()` in `MultipleComponentSerialization`). `SignalManager::Unregister` — miss (out of scope for this file; SignalManager lives elsewhere and IS hit in `RegistryTest.cpp`'s `SignalSystem` test, see below). Minor: last 3 tests use suite `ComponentRegistryReRegister` rather than `ComponentRegistryTest` (thematically consistent, not fixed). | - |
| tests/Reflection/FieldVisitorTest.cpp | 4 | 144 | GAP | Thin shell versus the `FieldInfo`/`IFieldVisitor` surface: covers field enumeration + `Serializable=false` exclusion, `GetAny`/`SetAny` round-trip, a null-slot case, and one attribute query. Missing despite being explicitly documented as supported: nested reflected-struct field recursion, container-typed fields (`std::vector`/`std::array`), the typed `Get<T>`/`Set<T>`/`GetPtr<T>` accessors, a single dual-mode (`IsWriting()`-branching) visitor, `SetAny` rejection for const/type-mismatch (negative path, rubric 7), and `HasAttribute`/`GetAttribute` for an *absent* attribute. | C |
| tests/Reflection/ReflectionTest.cpp | 43 | 944→945 | SPLIT (deferred) | 43 tests span ≥5 themes (TypeMeta, Field Access, Attributes, Enums, Lifecycle, Container Traits, JSON Schema, Registry Integration, MetaRegistry, ECS Integration) and would normally be an obvious split candidate. **Not split**: every theme shares the same `ASTRA_REFLECT_TYPE`/`ASTRA_REFLECT_ENUM` static registrations for `Position`/`Velocity`/`Health`/`Player`/`DamageType`/`StatusFlags` (confirmed via `MetaRegistry.hpp:360 StaticTypeRegistrar` — a static object performing **process-wide singleton registration** in its constructor). Splitting would require either duplicating these registrations across files (risking double-registration / static-init-order hazards in the shared `MetaRegistry` singleton) or extracting them to a shared header (a non-inline static registrar in a header still instantiates once per including TU, so the same hazard applies). This is exactly the "non-trivial fixture surgery that risks changing behavior" the audit's ambiguity resolution says to avoid — recorded as SPLIT-recommended, deferred rather than risked. | C (deferred split: proposed themes above; needs a design for safely sharing/gating the static registrations before it can be split, e.g. a single still-shared "reflection fixtures" file all split files `#include`) |
| tests/Registry/ArchetypeManagerTest.cpp | 16 | 521→520 | FIX | `SetUp` registered `Transform, Name, Physics, Player, Enemy` alongside `Position, Velocity, Health`, but no `TEST_F` body in the file ever uses the first five — dead fixture weight on every test run. Trimmed to `RegisterComponents<Position, Velocity, Health>()`. Also removed unused `#include <algorithm>/<numeric>/<random>/<unordered_set>`. Separately found but not touched (would need new assertions, not deletion): `StressManyArchetypes` conditionally adds `Health` but never re-checks it; `ComponentRegistrySharing` never asserts the original manager is unaffected by mutating a second manager sharing its registry; `EdgeCaching` only checks archetype *count*, never the entities' actual component values. `ArchetypeChunkPool::Defragment` (chunk-pool-level, distinct from `ArchetypeManager::Defragment`) is never exercised here. | B (dead registrations + unused includes removed, done) / C (missing value-assertions in `StressManyArchetypes`/`ComponentRegistrySharing`/`EdgeCaching`; `ArchetypeChunkPool::Defragment` miss) |
| tests/Registry/ArchetypeTest.cpp | 25 | 1090 | SPLIT (deferred) | Two clearly separable themes: core archetype mechanics (creation, add/remove/batch, chunk allocation/capacity/coalescing, component access, 21 tests) vs. serialization (`SerializeEmptyArchetype`/`SerializeWithEntities`/`SerializeMultipleChunks`/`SerializeNonTrivialComponents`, 4 tests, lines ~776-1091). **Not split**: the shared fixture (`ComponentRegistry` + `ArchetypeChunkPool` members + `GetDescriptors` helper) is ~32 lines — over the brief's "duplicate only if < 20 lines" threshold — and is used identically by every one of the 25 tests, so neither "move wholly to one file" (breaks the other file's `TEST_F` reference) nor "duplicate" (blocked by the line-count rule) is a clean fit without introducing a new shared header, which risks more surgery than the audit's zero-risk budget for this pass. Recorded as SPLIT-recommended, deferred. | C (deferred split: core (21 tests) vs. serialization (4 tests) as named above, blocked on a >20-line shared fixture) |
| tests/Registry/ParallelIterationTest.cpp | 4 | 124 | GAP | Confirmed: covers BOTH the injected-scheduler path (`InjectedSchedulerIsUsed`, `ParallelExecutorRunsAllSystems`) and the null-scheduler sequential fallback (`NoSchedulerFallsBackSequentially`, and the second half of `ParallelExecutorRunsAllSystems`). Missing: no test calls `ParallelForEach`/`ParallelFor` **nested inside** another `ParallelFor` callback running on a worker thread — `TestWorkerPool`'s `t_insideWorker` reentrancy guard (which exists specifically to prevent a nested-call deadlock) is therefore never actually exercised by this suite. | C (per seeded finding — add a nested-`ParallelFor`-from-worker-thread reentrancy test) |
| tests/Registry/RegistryLoadConfigTest.cpp | 2 | 73 | GAP | Confirmed: both tests (`RestoredRegistryKeepsWorkScheduler`, `TwoArgLoadWorksWithDefaultConfig`) only set `cfg.workScheduler`; neither drives a non-default `chunkPoolConfig` through Save/Load. Otherwise solid — deterministic, non-tautological, correctly located. | A (Task 10) |
| tests/Registry/RegistryTest.cpp | 23→18 | 814→~575 | SPLIT | Author-delineated: the file itself has a `// === Serialization Tests ===` section header at the old line 572 separating 18 core-operations tests from 5 serialization tests. Fixture is 19 lines (< 20) — safe to duplicate per the split policy. Split: extracted `EmptyRegistrySerialization`, `SingleEntitySerialization`, `MultipleEntitiesSerialization`, `RelationshipSerialization`, `SerializationErrorHandling` (byte-identical bodies) into new `tests/Registry/RegistrySerializationTest.cpp` with a duplicated fixture. Suite name unchanged (`RegistryTest` in both files). | B (done) |
| tests/Registry/RelationsTest.cpp | 16 | 560→561 | OK | Read in full to verify the seeded rejection-assertion concern for `CircularHierarchyHandling` (one of Task 1's 6 former Debug-aborters): it already asserts observable state correctly — `GetParent(a)` stays invalid after the rejected cycle AND the pre-existing chain (`GetParent(b)==a`, `GetParent(c)==b`) is explicitly re-checked intact, plus descendant/ancestor traversal counts. No strengthening needed; the seeded finding did not hold for this test once verified against the current source. | - |
| tests/Registry/RelationshipGraphSerializationTest.cpp | 10 | 536 | GAP | All 10 tests use only the memory-target writer/reader constructors — no file-target leg (mirrors the `EntityManagerSerializationTest.cpp` gap). No test drives the documented `Result::Err` path. Otherwise strong: `MixedRelationships` explicitly asserts an unrelated pair is NOT linked (good adjacent-state check), deterministic, stateless. | C |
| tests/Registry/RelationshipGraphTest.cpp | 16 | 451→452 | OK | Read in full to verify the seeded rejection-assertion concern for the 4 former Debug-aborters (`SelfLinks`, `CircularRelationshipPrevention`, `SelfLinkingPrevention`, `InvalidEntityOperations`): all four already assert observable state, not just absence-of-crash — e.g. `CircularRelationshipPrevention` re-checks the intact chain after each rejected cycle attempt at 2/3/4 levels; `InvalidEntityOperations` checks `GetParent`/`GetChildren`/`GetLinks` on the valid entity are all still empty after 4 different invalid-entity calls. No strengthening needed; the seeded finding did not hold once verified against the current source. | - |
| tests/Registry/ResourceTest.cpp | 18 | 505 | OK | SBO/heap (rubric 8) satisfied: `SmallResourceUseSBO` (16B, ≤ SBO_SIZE=64) and `LargeResourceUseHeap` (~112B, >64B) both present. Real assertions throughout; negative paths present (`GetNonExistentResource`, `RemoveNonExistentResource`). `ConcurrentReads` uses real threads with no sleeps. Minor gaps not fixed here (would need new assertions, not deletion): `TagResourceZeroMemory`'s name implies more than its body checks; `MixedSizeResources` only re-verifies 2 of 4 created resource types by readback. | C (add readback checks for the other 2 types in `MixedSizeResources`; tighten `TagResourceZeroMemory`; consider a `static_assert` locking the SBO/heap size boundary) |
| tests/Registry/ViewIteratorTest.cpp | 8 | 290 | GAP | `ModifyDuringIteration` constructs one view, mutates in place, then reuses the SAME view object for a second traversal — same systemic "reuse view across a component-membership mutation" pattern seeded for `ViewTest.cpp`, though this instance mutates values in place rather than adding/removing components, so it doesn't hit the specific empty-archetype bug Task 4 targets as directly. Otherwise solid: exact-value deterministic assertions, `EmptyView` is a good negative path, isolated fixture. | A (Task 4 — same `ViewInvalidationTest.cpp` remediation covers the view-cache-reuse pattern) |
| tests/Registry/ViewTest.cpp | 12 | 381→382 | GAP | Confirmed the seeded finding, precisely: `ViewInvalidation` (the one test that mutates after creating a view) only exercises entity moves *between two already-populated* archetypes — which the fast generation-counter path already handles correctly — never the specific bug scenario (view created while the matching archetype is empty, then populated without a "new archetype" event). No test calls `View::Size()`/`View::Empty()` anywhere in the file (confirmed via grep) — Public-API spot-check miss. Both gaps are closed by Task 4's new `ViewInvalidationTest.cpp` (confirmed from the remediation plan's Task 4 text, which explicitly asserts `view.Size()`/`view.Empty()` after the fix). | A (Task 4) |
| tests/Serialization/BinarySerializationTests.cpp | 28→22 | 1083→~845 | SPLIT | Two separable themes: core reader/writer + STL-container serialization (18 tests) vs. component-versioning/migration (`VersionedComponentWriteRead`, `VersionMigration`, `VersionTooOld`, `ComponentTypeMismatch`, `PODComponentVersioning`, `MultipleVersionedComponents`, 6 tests) plus checksum tests (kept in core). Fixture is 18 lines (< 20) — safe to duplicate. Split: extracted the 6 versioning tests (byte-identical bodies) into new `tests/Serialization/BinarySerializationVersioningTests.cpp` along with the `Position`/`CurrentFormatComponent` types and the `Astra::SerializationTraits<CurrentFormatComponent>` specialization they need (all anonymous-namespace-scoped in each file — the write/read round-trips are entirely local to a single test body, so cross-TU `TypeID` hash collisions, unlike the `ReflectionTest.cpp`/`TypeIDTests.cpp` cases, cannot affect correctness here). Also deleted `OldFormatComponent` — confirmed dead code (defined, never referenced anywhere in the original file). | B (split + dead-code deletion, done) |
| tests/Serialization/Compression/CompressionTests.cpp | 14 | 404→409 | FIX | Task 1 already gated `PerformanceBenchmark`'s throughput `EXPECT_GT`s behind `#ifdef NDEBUG` (Disposition A, done). Newly found: `GenerateRandomData` seeds with `std::random_device` (nondeterministic) rather than a fixed literal — violates rubric 5 — used only by `RandomData`, whose `EXPECT_LT(ratio, 1.5f)` is robust in practice (true-random bytes essentially never compress), so low severity; not fixed here (out of this task's narrow "relocate/split/strengthen-rejection-tests" mechanical scope). **Also newly found empirically during this task's own verification runs**: even NDEBUG-gated, `PerformanceBenchmark`'s `compressMBps > 10.0f` threshold flaked twice across 4 Release runs performed in this session (8.5-9.3 MB/s observed) under heavy unrelated system CPU load (confirmed via `Get-Counter` showing 99-100% CPU from unrelated processes) — passed cleanly once load subsided. This is environmental, not caused by any change in this task (file diff is empty), but shows the Task-1 fix didn't fully remove the flakiness, only confined it to Release. | C (nondeterministic seed in `GenerateRandomData`; loosen or remove the `PerformanceBenchmark` MB/s thresholds — they are measured-machine-dependent even after NDEBUG-gating) |
| tests/Support/TestWorkerPool.hpp | - | 153 | OK | Not a test file itself but a shared `IWorkScheduler` test-double used by `WorkSchedulerTest.cpp` and `ParallelIterationTest.cpp`. Correct condition-variable-based synchronization (no sleeps); `t_insideWorker` thread-local correctly documented as shared across all instances (acceptable for a test pool, noted in its own comment). No dead code. | - |
| tests/Container/FlatMapInternalsTest.cpp (new, from split) | 6 | ~230 | OK | New file from the `FlatMapTest.cpp` split (see above). Byte-identical bodies moved, not independently re-audited beyond the split verification. | - |
| tests/Registry/RegistrySerializationTest.cpp (new, from split) | 5 | ~245 | OK | New file from the `RegistryTest.cpp` split (see above). Byte-identical bodies moved, not independently re-audited beyond the split verification. | - |
| tests/Serialization/BinarySerializationVersioningTests.cpp (new, from split) | 6 | ~240 | OK | New file from the `BinarySerializationTests.cpp` split (see above). Byte-identical bodies moved, not independently re-audited beyond the split verification. | - |

## Known findings seeded from the 2026-07-10 source review
(verify each during the per-file pass; do not re-derive)
- ViewTest.cpp: every test constructs a fresh view after mutation — cached-view
  contracts untested. Disposition A: Task 4 adds ViewInvalidationTest.
  **Verified**: confirmed precisely — see ViewTest.cpp row above. Also confirmed
  View::Size/Empty are never called anywhere in the file.
- CompressionTests.cpp: perf thresholds in unit tests. Disposition A: Task 1.
  **Verified**: Task 1's NDEBUG gate is in place and correct. New finding layered
  on top: the gated threshold is still flaky under CPU contention (see row above).
- RelationshipGraphTest/RelationsTest/ComplexRelationshipTest rejection tests:
  now that Task 1 removed the Debug asserts, confirm each asserts OBSERVABLE
  state after rejection (GetParent unchanged, no link added) — not just
  "did not crash". Any that only prove absence-of-crash: Verdict FIX,
  Disposition B (strengthen the assertion in place).
  **Verified**: 5 of the 6 former Debug-aborters already had strong,
  observable-state assertions (RelationshipGraphTest's SelfLinks,
  CircularRelationshipPrevention, SelfLinkingPrevention, InvalidEntityOperations;
  RelationsTest's CircularHierarchyHandling) — the seeded finding did not hold
  for those once read against current source. Only
  ComplexRelationshipTest.RapidRelationshipChanges genuinely needed
  strengthening (a 1000-op randomized stress test that only checked
  "no crash"); fixed in place with a post-loop no-cycle-exists invariant check.
- TestCommandBuffer.cpp: lives at tests/ root; every other suite is foldered.
  Disposition B: move to tests/Commands/CommandBufferTest.cpp.
  **Done.**
- No test drives a non-default chunkPoolConfig through Save/Load.
  Disposition A: Task 10. **Verified** in RegistryLoadConfigTest.cpp.
- No coverage: NSDMI defaults / Release zeroing (Task 3), alignas payloads in
  CommandBuffer (Task 5), entity exhaustion & dead-handle batch adds (Task 6),
  tags in range-for (Task 7), ComponentRemoved pointer lifetime (Task 8),
  Delegate large functors (Task 9), ID-space overflow (Task 11), 16/64-bit
  entity widths (Task 12), archive portability (Task 13), resource
  persistence (Task 14). All Disposition A. **Confirmed** for the ones checked
  during the per-file pass (Task 3 via TestComponents.hpp's Health; Task 6/12
  via EntityManagerTest.cpp/EntityTest.cpp).
- Fuzz tests (Bitmap/FlatMap/FlatSet/SmallVector): confirm fixed seeds and a
  reference-container oracle (std::unordered_map / std::set / std::vector
  comparison). Missing oracle or nondeterministic seed: Verdict FIX.
  **Verified**: all 8 fuzz files (Bitmap, BitmapFuzz, FlatMapFuzz, FlatSetFuzz,
  SmallVectorFuzz, plus the fuzz-adjacent RandomOperations in FlatMapTest)
  use fixed literal seeds and a reference-container oracle checked every
  operation. No fixes needed.
- TestComponents.hpp (332 lines, shared by many suites): flag unused component
  types and any component whose semantics assume Debug-only zeroing.
  **Done** — see TestComponents.hpp row: Inventory/Timer/Hierarchy/Metadata
  unused; Health is the Task-3-shaped NSDMI case, currently untested at the
  value level.
- ParallelIterationTest.cpp (4 tests): confirm it covers BOTH the injected-
  scheduler path and the null-scheduler sequential fallback, and nested
  ParallelFor reentrancy. Missing legs: Verdict GAP, Disposition C.
  **Verified**: both scheduler-path legs covered; nested-reentrancy leg
  missing (Disposition C, see row above).

## Public-API spot-check (record hits/misses)
- `Registry::Defragment` + `DefragmentationResult` fields: **HIT** (MemoryCleanupTest.cpp, ComponentLifecycleTest.cpp (partial — result discarded), ResourceExhaustionTest.cpp, RegistryTest.cpp's `ArchetypeCleanup`).
- `Registry::GetFragmentationLevel`: **MISS** — not called anywhere in the suite.
- `CommandBuffer::MergeFrom`: **MISS**.
- `ParallelCommandBuffer::MergeInto`/`Clear`/`GetThreadCount`: **MISS** — `CommandBufferTest.cpp`'s `ParallelCommandBuffer` test only exercises `GetThreadBuffer`+`Execute`.
- `View::Size`/`Empty`: **MISS** in `ViewTest.cpp` today; will be **HIT** once Task 4 lands `ViewInvalidationTest.cpp` (confirmed from the remediation plan's Task 4 test text).
- `EntityManager::ShrinkToFit`: **MISS** — not called anywhere in the suite.
- `ArchetypeChunkPool::Defragment` (chunk-pool level, distinct from `ArchetypeManager::Defragment`): **MISS** — confirmed absent from `MemoryCleanupTest.cpp` and `ArchetypeManagerTest.cpp`, the two files most likely to exercise it.
- `SignalManager` handler `Unregister`: **HIT** — `RegistryTest.cpp`'s `SignalSystem` test calls `signals->On<...>().Unregister(...)` for both `EntityCreated` and `ComponentAdded` handlers and confirms events stop firing afterward.
- `EnumInfo` flag enums: **HIT** — `ReflectionTest.cpp`'s `FlagsEnum` test checks `enumInfo->isFlags` on `StatusFlags` (an `ASTRA_REFLECT_ENUM_FLAGS()` enum).
- `JsonSchema` generation for nested types: **MISS** — `ReflectionTest.cpp`'s 3 JSON-schema tests all target flat `Position` (float x/y/z); no test generates a schema for a type containing another reflected type as a field.

## Deferred items filed under this audit (Category C, appended to the plan's Deferred section)
See `docs/superpowers/plans/2026-07-10-astra-remediation.md`, bullet
**"Test-coverage follow-ups (from Task 2 audit)"**, for the itemized list
(one line per finding above with Disposition C).
